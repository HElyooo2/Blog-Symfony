{% extends 'base.html.twig' %}
{% block body %}

        <table class="table">
        
            <tbody>
       
                {% for post in posts %} 
                <tr>
                    <th>Nom: </th>
                    <td ><a href="/post/{{ post.id }}">{{ post.nom }}</a></td>
                </tr>
                
                 {% endfor %}      
            </tbody>
        </table>

{% endblock %}