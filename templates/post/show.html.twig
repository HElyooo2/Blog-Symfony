{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
{{ parent() }}
  
  <div class="row">
    <div class="leftcolumn">
        <div class="card">
            <table class="table">
                <tbody>
    
                    <tr>
                        <th></th>
                        <td><h1>{{ post.nom }} </h1><h3>Posté le: {{ post.createdAt ? post.createdAt|date('Y-m-d') : '' }}</h3></td>
                    </tr>
                    {% if not post.tags.empty %}
                    <tr>
                      <th>Tags:</th>
                      <td>{% for tag in post.tags %}<a href="/tag/{{ tag.id }}" class="badge {{ tag.categ }}">{{ tag.name }}</a>  {% endfor %}</td>
                    </tr>
                    {% endif %}
                    <tr>
                        <th></th>                        
                        <td>{{ post.content|markdown_to_html }}</td>
                    </tr>
                    
                    <tr>
                        <th>Image</th>
                        <td>{{ post.image }}</td>
                    </tr>
                </tbody>
            </table>
        </div
          </div>
  
    </div>
    <div class="rightcolumn">
      
      <div class="Ressources:">
        <h3>Vouvez me suivre ici:</h3>
        <p><a href="gogle.com">Repository GitHub</a></p>
        <p><a href="gogle.com">Sujet</a></p>
        <p><a href="gogle.com">Trello</a></p>
      </div>
      <div class="card">
        <h3>Vouvez me suivre ici:</h3>
        <p><a href="gogle.com">Linkdin</a> ajouter logo</p>
        <p><a href="gogle.com">Github</a> ajouter logo</p>
        <p><a href="gogle.com">Twitter</a> ajouter logo</p>
      </div>
    </div>
  </div>

{% endblock %}
