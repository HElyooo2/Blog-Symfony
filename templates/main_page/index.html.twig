{% extends 'base.html.twig' %}

{% block title %}Hello MainPageController!{% endblock %}

{% block body %}
{{ parent() }}
  <div id="sidebar"></div>  

  <div class="row">
    
    <div class="leftcolumn">
      
        {% for post in posts %}

        {# <div class="card">
            <h2><a href="{{ path('app_post_show', {'id': post.id}) }}">{{ post.title }} </a></h2>
            <h5>Posté le: {{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</h5>
            {% for tag in post.tags %}<a href="/tag/{{ tag.id }}" class="badge {{ tag.categ }}">{{ tag.name }}</a>  {% endfor %}
            <!-- <div class="fakeimg" style="height:200px;" href="{{ post.image }}">Image</div> -->
            <p>{{ post.content|slice(0, 255) }}...</p><a href="/post/{{ post.id }}" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Lire la suite</a>
          </div> #}
        <div class="card">
          <div class="row">
            <div class="col">
              <h2><a href="{{ path('app_post_show', {'id': post.id}) }}">{{ post.title }} </a></h2>
              <h5>Posté le {{ post.createdAt ? post.createdAt|date('Y-m-d') : '' }}</h5>
              {% for tag in post.tags %}<a href="/tag/{{ tag.id }}" class="badge {{ tag.categ }}">{{ tag.name }}</a>  {% endfor %}
            </div>
            <div class="col">
              <div class="bg-image hover-overlay ripple shadow-1-strong rounded" data-mdb-ripple-color="light">
              <img src="\images\test.png" class="w-100" alt="Louvre" />
              <a href="#!">
              <div class="mask" style="background-color: rgba(251, 251, 251, 0.2)"></div>
              </a>
            </div>
              {# <img src="\images\test.png" class="img-fluid img-thumbnail" alt="img-thumbnail"> #}
            </div>
          </div>
          <div class="row">
            <div class="col">
            <p>{{ post.content|slice(0, 255) }}...</p><a href="/post/{{ post.id }}" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Lire la suite</a>
              
            </div>
          </div>
        </div>
    

  

        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}

    </div>

        
    <div class="rightcolumn">
      <div class="card">
        <h2>A propos:</h2>
        <div class="fakeimg"><img src="\images\moimeme.jpg"  class="img-fluid" alt="c moi"></div>
        <p>Bonjour, je m'appelle Hélio Gaudé et je suis étudiant en BTS SIO. Voici mon <a href="google.com" >CV</a>.</p>
      </div>
      
      <div class="card">
        <h3>Vouvez me suivre ici:</h3>
        <p><a href="gogle.com"><img src="\logos\twiter.png"  class="img-fluid" alt="c moi"></a> ajouter logo</p>
        <p><a href="gogle.com"><i class="fa fa-github" aria-hidden="true"></i>Github</a> ajouter logo</p>
        <p><a href="gogle.com">Twitter</a> ajouter logo</p>
      </div>
    </div>
  </div>

{% endblock %}
